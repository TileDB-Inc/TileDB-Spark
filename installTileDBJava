#!/bin/bash
mkdir build
pushd build
if [ -d "TileDB-Java" ]; then
  rm -r ./TileDB-Java
fi
git clone https://github.com/TileDB-Inc/TileDB-Java.git
pushd TileDB-Java
git checkout master
./gradlew assemble publishToMavenLocal --stacktrace --debug --info
popd
popd
